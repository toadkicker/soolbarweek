<nav class="navbar navbar-light navbar-toggleable-md affix-top">
  <div class="container">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse"
            data-target="#navbarNav"
            aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="navbar-collapse collapse" id="navbarNav">
      <div class="navbar-nav">
        <% nav.each do |item| %>
            <%= link_to item[:title], item[:url], class: "nav-item nav-link #{item[:active]}" %>
        <% end %>
      </div>
    </div>
    <% if current_user %>
        <%= link_to t('sign_out'), destroy_user_session_path, data: {:confirm => 'Are you sure?'}, :method => :delete, class: 'btn btn-danger mr-2' %>
    <% else %>
        <%= link_to t('sign_in'), new_user_session_path, class: 'btn btn-info mr-2' %>
    <% end %>
    <%= link_to t('wristbands'), '#', class: 'btn btn-success' %>
  </div>
</nav>